services:
  app:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./io:/app/io
      - ./credentials:/app/credentials:ro
    command: ["sync"]

  web:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./io:/app/io
      - ./credentials:/app/credentials:ro
    command: ["uvicorn", "src.app:app", "--host", "0.0.0.0", "--port", "8000"]
